{
  "name": "forge",
  "version": "1.0.0",
  "private": true,
  "description": "Time Tracker with Google Spreadsheet integration",
  "scripts": {
    "format": "biome format --write .",
    "format:check": "biome check .",
    "install:safe": "pnpm install --frozen-lockfile --ignore-scripts",
    "supply-chain:check": "node scripts/supply-chain/check-install-risk.mjs",
    "lint": "pnpm --filter forge-app lint && pnpm --filter api lint",
    "lint:fix": "pnpm --filter forge-app lint:fix && pnpm --filter api lint:fix",
    "lint:app": "pnpm --filter forge-app lint",
    "lint:worker": "pnpm --filter api lint"
  },
  "engines": {
    "node": ">=20.12.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10"
  }
}
